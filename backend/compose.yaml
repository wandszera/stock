# compose.yaml
services:
  db:
    image: postgres:16
    container_name: estoque_db
    environment:
      POSTGRES_DB: estoque
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: