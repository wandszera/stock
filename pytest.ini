[pytest]
addopts = -p no:cacheprovider
testpaths = backend/tests
norecursedirs = .* build dist node_modules backend/env backend/pytest-cache-files-* pytest-cache-files-*
markers =
    postgres: testes de integracao e concorrencia que exigem PostgreSQL
